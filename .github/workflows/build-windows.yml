name: Build Windows Plugin

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Configure CMake
        run: |
          cmake -B build -G "Visual Studio 17 2022" -A x64 `
            -DKILLERB_COPY_AFTER_BUILD=OFF

      - name: Build VST3
        run: cmake --build build --config Release --target KillerBSynth_VST3

      - name: Package VST3
        run: |
          $vst3 = Get-ChildItem -Recurse -Filter "*.vst3" -Directory | Select-Object -First 1
          if (-not $vst3) { throw "VST3 not found" }
          Compress-Archive -Path $vst3.FullName -DestinationPath "$env:GITHUB_WORKSPACE\Killer-B-Synth-Windows-VST3.zip" -Force

      - name: Upload Windows VST3
        uses: actions/upload-artifact@v4
        with:
          name: Killer-B-Synth-Windows-VST3
          path: Killer-B-Synth-Windows-VST3.zip
          if-no-files-found: error
